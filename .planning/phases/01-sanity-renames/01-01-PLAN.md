---
phase: 01-sanity-renames
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/sanity/schemaTypes/Work.ts
  - packages/sanity/deskStructure.ts
autonomous: true
requirements:
  - RENAME-01
  - RENAME-02

must_haves:
  truths:
    - "Sanity Studio displays 'Collection' as the document type title (not 'Work')"
    - "Sanity Studio desk structure shows 'Archive' as a navigation list item"
    - "Creating and editing collection documents works normally after rename"
  artifacts:
    - path: "packages/sanity/schemaTypes/Work.ts"
      provides: "Collection document type with renamed title"
      contains: "title: 'Collection'"
    - path: "packages/sanity/deskStructure.ts"
      provides: "Archive navigation item in desk structure"
      contains: "title('Archive')"
  key_links:
    - from: "packages/sanity/deskStructure.ts"
      to: "packages/sanity/schemaTypes/Work.ts"
      via: "Archive list item filters on _type == 'work' which is the unchanged name identifier from the Work schema"
      pattern: "params.*type.*work"
---

<objective>
Rename the Sanity CMS document type display title from "Work" to "Collection" and add an "Archive" navigation item to the Studio desk structure.

Purpose: The Studio UI should reflect the new Archive/Collection naming so content editors see the correct labels when managing collection documents.
Output: Two edited files — `Work.ts` with updated title, `deskStructure.ts` with new Archive list item.
</objective>

<execution_context>
@/Users/rasmus/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rasmus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-sanity-renames/01-RESEARCH.md

@packages/sanity/schemaTypes/Work.ts
@packages/sanity/deskStructure.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename document type title from "Work" to "Collection"</name>
  <files>packages/sanity/schemaTypes/Work.ts</files>
  <action>
In `packages/sanity/schemaTypes/Work.ts`, change line 4 from `title: 'Work'` to `title: 'Collection'`.

Do NOT change:
- `name: 'work'` — this is the internal document type identifier used in all GROQ queries, existing documents, and generated types. Changing it would break everything.
- The filename `Work.ts` — the module is imported by name in `schemaTypes/index.ts`. Renaming the file is out of scope.
- Any other field in the schema.

The only edit is the `title` property value string.
  </action>
  <verify>
    <automated>grep -q "title: 'Collection'" packages/sanity/schemaTypes/Work.ts && grep -q "name: 'work'" packages/sanity/schemaTypes/Work.ts && echo "PASS" || echo "FAIL"</automated>
    <manual>After restarting Sanity Studio, the "New document" dropdown should show "Collection" instead of "Work".</manual>
  </verify>
  <done>Work.ts has `title: 'Collection'` and `name: 'work'` is unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Add Archive list item to desk structure</name>
  <files>packages/sanity/deskStructure.ts</files>
  <action>
In `packages/sanity/deskStructure.ts`, add a new Archive list item to the `S.list().items([...])` array. Place it after the Products entry (the last current item) with an `S.divider()` before it for visual separation.

Add these lines before the closing `]);` of the items array:

```typescript
S.divider(),
S.listItem()
    .title('Archive')
    .icon(MdWork)
    .child(
        S.documentList()
            .title('Archive')
            .showIcons(true)
            .filter('_type == $type')
            .params({ type: 'work' })
    ),
```

`MdWork` is already imported at the top of the file — no new import needed.

This follows the exact same document list pattern used by Releases, Videos, and other entries in the file. The filter uses `_type == 'work'` because the internal `name` identifier has not changed.

Do NOT:
- Remove or modify any existing list items
- Change the import statements (MdWork is already imported)
- Add ordering (works/collections don't have a date field to sort by)
  </action>
  <verify>
    <automated>grep -q "title('Archive')" packages/sanity/deskStructure.ts && grep -q "params({ type: 'work' })" packages/sanity/deskStructure.ts && echo "PASS" || echo "FAIL"</automated>
    <manual>After restarting Sanity Studio, the left navigation should show an "Archive" item at the bottom. Clicking it should list all collection (work) documents.</manual>
  </verify>
  <done>deskStructure.ts contains an Archive list item that filters on `_type == 'work'` and appears after a divider following Products.</done>
</task>

</tasks>

<verification>
1. `packages/sanity/schemaTypes/Work.ts` contains `title: 'Collection'` and still contains `name: 'work'`
2. `packages/sanity/deskStructure.ts` contains an Archive list item with `.title('Archive')` and `.params({ type: 'work' })`
3. No other files in `packages/sanity/` have been modified
4. TypeScript compilation of the Sanity package passes (no syntax errors introduced)
</verification>

<success_criteria>
- The Work.ts schema `title` property reads 'Collection'
- The Work.ts schema `name` property still reads 'work' (unchanged)
- The deskStructure.ts file has a new Archive list item filtering on work documents
- Both files are syntactically valid TypeScript
</success_criteria>

<output>
After completion, create `.planning/phases/01-sanity-renames/01-01-SUMMARY.md`
</output>
