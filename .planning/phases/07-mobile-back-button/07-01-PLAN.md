---
phase: 07-mobile-back-button
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/sveltekit/src/lib/components/Works/WorkTopBar.svelte
autonomous: true
requirements: [NAV-01, NAV-02, NAV-03]

must_haves:
  truths:
    - "On mobile, the archive top bar shows an X element on the left side"
    - "Tapping the X navigates to /#archive"
    - "The X element has the same width as the bottom bar arrow sections on mobile and a right border"
    - "The archive collection title is perfectly horizontally centered with the back element present"
  artifacts:
    - path: "packages/sveltekit/src/lib/components/Works/WorkTopBar.svelte"
      provides: "Mobile back button and centered title layout"
      contains: "×"
  key_links:
    - from: "WorkTopBar .back a"
      to: "/#archive"
      via: "href attribute"
      pattern: 'href="/#archive"'
---

<objective>
Add a mobile "X" back element to the archive top bar with centered title layout.

Purpose: Mobile users currently have no way to navigate back to the archive list from a collection page. This adds a visible "X" close element that links to /#archive, while keeping the title perfectly centered.

Output: Updated WorkTopBar.svelte with mobile back button and balanced centering layout.
</objective>

<execution_context>
@/Users/rasmus/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rasmus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@packages/sveltekit/src/lib/components/Works/WorkTopBar.svelte
@packages/sveltekit/src/lib/components/Works/WorkBottomBar.svelte
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add mobile back button and centered title layout to WorkTopBar</name>
  <files>packages/sveltekit/src/lib/components/Works/WorkTopBar.svelte</files>
  <action>
Modify WorkTopBar.svelte to show a mobile "X" back element with centered title:

**Markup changes:**
1. Change the `.back` element's inner text from "Archive" to `×` (HTML entity `&times;`). The desktop layout already shows this element at `width: 240px` with `border-right`, so desktop continues working. On mobile, we will now SHOW it instead of hiding it.
2. Add a spacer `<div class="spacer"></div>` after `.view-selection-outer` (or after `.title-section` on mobile). This spacer mirrors the back element's width on mobile to keep the title centered.

**CSS changes inside the `@media (max-width: 800px)` block:**

For `.back`:
- REMOVE `display: none` — the back element must be visible on mobile
- Set `width: auto` and `padding: 0.2em 1em` to match the bottom bar `.side` button sizing on mobile
- Keep `border-right: 1px solid var(--archive-border-color)` (already set in the base rule)
- Remove `order: 1` (no longer needed, keep natural DOM order: back, title, spacer)

For `.spacer`:
- On desktop: `display: none` (desktop layout uses the existing `.back` + `.view-selection-outer` at 240px each for symmetry)
- On mobile: `display: flex; width: auto; padding: 0.2em 1em;` matching the back element sizing exactly, so flex centering of `.title-section` is balanced. The spacer has no content and no border — it is purely a width-matching invisible counterweight.

For `.title-section`:
- Keep existing mobile styles (`padding: 0.4em`, `width: 100%` can be replaced with `flex: 1` so it fills remaining space between back and spacer, `user-select: none`, `pointer-events: auto`, `cursor: pointer`)
- Change `width: 100%` to `flex: 1` in the mobile media query so the title expands to fill between back and spacer rather than forcing 100% width

For `.view-selection-outer`:
- Keep `display: none` on mobile (unchanged)

For `.mobile-indicator`:
- Keep `display: flex` on mobile (unchanged)

**Important details:**
- The `×` character should be the actual Unicode multiplication sign (×, U+00D7) or HTML entity `&times;` for clean rendering, not a plain letter "x"
- The `<a>` tag inside `.back` already has `href="/#archive"` — keep that
- Desktop layout (>800px) is unaffected: `.back` shows `×` at 240px with border-right, `.spacer` is hidden, `.view-selection-outer` shows RadioGroup at 240px
- On mobile the three-section flex layout becomes: `[×] [Title...] [spacer]` where `[×]` and `[spacer]` have identical sizing, ensuring the title is perfectly centered
  </action>
  <verify>
    <automated>cd /Users/rasmus/Documents/dev/worldaffairs/worldaffairs.se && npx svelte-check --workspace packages/sveltekit 2>&1 | tail -5</automated>
    <manual>On mobile viewport (≤800px): back X visible on left with right border, title centered, tapping X navigates to /#archive. On desktop (>800px): layout unchanged, back section shows × at 240px width.</manual>
  </verify>
  <done>
    - Mobile archive top bar shows × back element on left side with right border
    - × links to /#archive
    - × element sizing matches bottom bar arrow sections on mobile (auto width, 0.2em 1em padding)
    - Title text is perfectly horizontally centered via matching spacer on the right
    - Desktop layout remains unchanged (back at 240px, RadioGroup at 240px, title centered between them)
  </done>
</task>

</tasks>

<verification>
1. `npx svelte-check --workspace packages/sveltekit` passes with 0 errors
2. Mobile (≤800px): × element visible on left, title centered, × tappable navigating to /#archive
3. Desktop (>800px): Layout unchanged from Phase 6 state — back shows ×, RadioGroup visible, title centered
4. The × element and spacer have identical mobile sizing (auto width, 0.2em 1em padding)
</verification>

<success_criteria>
- NAV-01: Mobile top bar shows × back element linking to /#archive
- NAV-02: Back element has same width as bottom bar arrow sections and has a right border
- NAV-03: Title is perfectly horizontally centered with back element present (verified by matching spacer)
</success_criteria>

<output>
After completion, create `.planning/phases/07-mobile-back-button/07-01-SUMMARY.md`
</output>
